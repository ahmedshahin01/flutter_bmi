//import 'package:flutter/material.dart';
//
/*

class Myres2 extends StatelessWidget {
   double resultnum ;
   bool gender;

  Myres2(this.resultnum,this.gender);
   String get resultPhrase {
    String resultText = '';
    if (resultnum >= 30)
      resultText = 'Obese';
    else if (resultnum > 25 && resultnum < 30)
      resultText = 'Overweight';
    else if (resultnum >= 18.5 && resultnum <= 24.9)
      resultText = 'Normal';
    else
      resultText = 'Thin';
    return resultText;
  }

  @override
  Widget build(BuildContext context) {
    return Column(
      
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        Text(resultPhrase)
      ],
    );
  }
}*/